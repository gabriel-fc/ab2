# graph-theories-ab2-project


This repository keeps the implementations of the algorithms requested to the AB2 tests. To generate the executable file, run 'make' command at the algorithm's directory.
